import React from 'react';
import Lists from './list';

const All = () => {
  const [state, setState] = React.useState({});
  return (
    <div>
      <Lists />
    </div>
  );
};

export default All;
